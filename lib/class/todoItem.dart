class TodoItem {
  String title;
  String description;
  String subject;
  DateTime endDate;
  bool isDone;
  TodoItem({
    required this.title,
    required this.description,
    required this.subject,
    required this.endDate,
    required this.isDone,
  });
}